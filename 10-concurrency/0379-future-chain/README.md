# 0379 — Future Chain

Chain asynchronous steps 5 -> (*2) -> (+1) and print the final value `11`. Each step runs on a global `DispatchQueue` and a `DispatchGroup` joins the result before the next.

## Run

    swift main.swift
