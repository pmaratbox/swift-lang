# 0076 — Count Vowels

Count the vowels (`a`, `e`, `i`, `o`, `u`) in the word `hello` and print the total: `2`. A `Set<Character>` of vowels gives O(1) membership; `filter` keeps the vowels and `.count` tallies them.

## Run

    swift main.swift
