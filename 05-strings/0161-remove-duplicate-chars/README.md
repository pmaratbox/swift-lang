# 0161 — Remove Duplicate Characters

Remove duplicate characters from "aabbcc" keeping first occurrence, printing `abc`. `Set.insert` returns `inserted`, letting one `where` clause both test and record seen characters.

## Run

    swift main.swift
