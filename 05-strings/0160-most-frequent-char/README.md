# 0160 — Most Frequent Character

Find the most frequent character in "hello", printing `l`. Tally counts in a `[Character: Int]` dictionary, then scan in order to pick the max deterministically.

## Run

    swift main.swift
