# 0156 — Count Substring Occurrences

Count non-overlapping occurrences of "ab" in "ababab", printing `3`. Scan a `Character` array, comparing slices and skipping past each match found.

## Run

    swift main.swift
