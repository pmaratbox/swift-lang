# 0072 — Palindrome Check

Check whether each of `level` and `hello` reads the same forwards and backwards, printing `level: yes` and `hello: no`. `s.reversed()` reverses the `Character`s (grapheme clusters); rebuilding a `String` and comparing tests the palindrome.

## Run

    swift main.swift
