# 0088 — String Builder

Use a string builder to assemble `1`, `2`, and `3` joined by `-`, printing `1-2-3`. A Swift `String` is a mutable value type; `+=` appends in place (amortized O(1)), so it doubles as the builder.

## Run

    swift main.swift
