# 0159 — String Interpolation

Interpolate name="Ada" and age=36 into a sentence, printing `Ada is 36`. Swift's `\(...)` interpolation embeds any value directly inside a string literal.

## Run

    swift main.swift
