# 0075 — Anagram Check

Check whether each pair is an anagram by comparing sorted letters — `listen`/`silent` and `hello`/`world` — printing `listen/silent: yes` and `hello/world: no`. `String.sorted()` returns a sorted `[Character]`; comparing the two arrays decides the anagram.

## Run

    swift main.swift
