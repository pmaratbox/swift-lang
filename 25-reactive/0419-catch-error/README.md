# 0419 — Catch Error

Implement catchError that, on an error from the source, switches to a fallback stream. The error closure simply re-subscribes the same observer to the fallback Observable.

## Run

    swift main.swift
