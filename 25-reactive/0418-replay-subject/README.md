# 0418 — ReplaySubject

Implement a ReplaySubject with a buffer of the last 2 values, replayed to a late subscriber, which then also receives new values. The buffer is trimmed with `removeFirst` so it never exceeds the configured size.

## Run

    swift main.swift
