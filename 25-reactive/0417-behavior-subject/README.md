# 0417 — BehaviorSubject

Implement a BehaviorSubject that holds a current value and replays it immediately to each new subscriber. A `final class` stores the current value and an array of observer closures, replaying on `subscribe`.

## Run

    swift main.swift
