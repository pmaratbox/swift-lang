# 0406 — Filter Operator

Implement a filter operator that forwards only values passing a predicate, keeping the even numbers of 1..6. Generic `Observer`/`Observable` structs wrap escaping closures so the operator stays composable and type-safe.

## Run

    swift main.swift
