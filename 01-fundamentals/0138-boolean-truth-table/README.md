# 0138 — Boolean Truth Table

For each (a,b) in (T,T),(T,F),(F,T),(F,F) print a, b, and a AND b, a OR b, a XOR b as lowercase booleans, one row per line. Swift's `Bool` interpolates as lowercase `true`/`false`, and `!=` doubles as XOR.

## Run

    swift main.swift
