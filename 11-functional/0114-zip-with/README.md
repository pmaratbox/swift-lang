# 0114 — Zip With

Combine [1,2,3] and [4,5,6] element-wise with addition, printing `5 7 9`. Swift pairs the arrays with `zip` then `map`s the `+` operator over each tuple.

## Run

    swift main.swift
