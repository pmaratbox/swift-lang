# 0386 — Iterate and Take

Iterate f(x)=x*3 from 1 and take the first four values, printing `1 3 9 27`. Swift's `sequence(first:next:)` lazily iterates and `prefix` takes the first four.

## Run

    swift main.swift
