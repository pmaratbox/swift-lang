# 0078 — Substring Search

Find the index of the first occurrence of `lo` in `hello` and print it: `3`. `firstRange(of:)` (a stdlib method, no Foundation needed) returns a `Range<String.Index>`; `distance(from:to:)` converts the start index into an integer offset.

## Run

    swift main.swift
