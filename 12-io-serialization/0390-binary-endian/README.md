# 0390 — Big-Endian Bytes

Encode the integer 258 as two big-endian bytes (1, 2), decode them back to 258, printing `1 2 258`. Use bit-shifting and masking to split the high and low bytes, then recombine.

## Run

    swift main.swift
