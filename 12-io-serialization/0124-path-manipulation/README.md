# 0124 — Path Manipulation

Join "/tmp" and "file.txt", then take the basename and extension, printing `/tmp/file.txt file.txt .txt`. `NSString`'s path methods handle joining, basename, and extension without manual separator logic.

## Run

    swift main.swift
