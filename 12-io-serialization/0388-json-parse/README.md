# 0388 — JSON Parse

Parse the JSON object `{"x":1,"y":2}` and print its entries `x=1 y=2`. Hand-parse the flat object by splitting on commas and colons, then format each key=value pair.

## Run

    swift main.swift
