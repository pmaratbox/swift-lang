# 0203 — Prototype (Clone)

Clone a prototype object holding 1, mutate the clone to 2, and show the original is unchanged, printing `1 2`. A reference type with an explicit `clone()` makes the deep copy intent clear in Swift.

## Run

    swift main.swift
