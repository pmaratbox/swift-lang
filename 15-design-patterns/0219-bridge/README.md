# 0219 — Bridge

Bridge a Circle abstraction to a Red color implementation, printing `red circle`. The `Shape` abstraction holds a `Color` implementor protocol, letting the two hierarchies vary independently.

## Run

    swift main.swift
