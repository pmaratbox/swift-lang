# 0202 — Abstract Factory

Use a "dark" theme abstract factory to build a button and a checkbox, printing `dark-button dark-checkbox`. A `ThemeFactory` protocol returns related product protocols whose conforming types carry the theme prefix.

## Run

    swift main.swift
