# 0204 — Adapter

Adapt a Celsius source (100) to a Fahrenheit interface, printing `212`. The adapter struct wraps the source and conforms to the target `FahrenheitProvider` protocol.

## Run

    swift main.swift
