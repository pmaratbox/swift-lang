# 0213 — Iterator (Custom)

Implement a custom iterator over a range 1..3 and consume it, printing `1 2 3`. Conforming to `IteratorProtocol` and `Sequence` plugs the custom iterator into Swift's `for`/collection machinery.

## Run

    swift main.swift
