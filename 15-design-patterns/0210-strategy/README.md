# 0210 — Strategy

Select an "add" then a "mul" strategy and apply each to (3,4), printing `7 12`. Swift closures are first-class, so a strategy is just a `(Int, Int) -> Int` function value.

## Run

    swift main.swift
