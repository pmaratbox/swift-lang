# 0061 — Reverse a String

Reverse the string `abc` character by character and print the result: `cba`. Swift's `reversed()` works on `Character`s — grapheme clusters — so it correctly reverses even combined emoji or accented letters, unlike byte or UTF-16 reversal.

## Run

    swift main.swift
