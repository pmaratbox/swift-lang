# 0431 — Concat Streams

Use the library's concat operator so the second stream starts only after the first completes. Uses Apple Combine's `Publishers.Concatenate(prefix:suffix:)` to join two publishers.

## Run

    swift main.swift
