# 0436 — Debounce (Virtual Time)

Use the library's debounce operator on a virtual/test scheduler to emit a value only after a quiet window. Built with Apple Combine's `debounce(for:scheduler:)` driven by a custom virtual-time `Scheduler`.

## Run

    swift main.swift
