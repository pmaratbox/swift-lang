# 0433 — Zip Streams

Use the library's zip operator to pair two streams by index and combine each pair as a+b. Apple Combine's `zip` pairs the two publishers by index and `map` combines each pair.

## Run

    swift main.swift
