# 0429 — Take Operator

Use the library's take operator to take the first 3 values of an unbounded stream, then complete. Built with Apple Combine using `prefix(3)` (Combine's take) on a lazily-pulled unbounded sequence publisher.

## Run

    swift main.swift
