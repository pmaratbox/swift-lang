# 0426 — Map Operator

Use the library's map operator to double each value of a 1..4 stream. Built with Apple Combine: an array `.publisher` piped through `.map` and collected via `.sink`.

## Run

    swift main.swift
