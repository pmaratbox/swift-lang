# 0425 — Create a Stream

Create a stream of 1, 2, 3 with your language's reactive library and print each value, then print done on completion. Uses Apple Combine's `[Int].publisher` sequence publisher with `sink`, printing each value and "done" from the completion callback.

## Run

    swift main.swift
