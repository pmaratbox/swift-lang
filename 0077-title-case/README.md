# 0077 — Title Case

Convert `hello world` to title case (uppercase the first letter of each word) and print it: `Hello World`. `prefix(1).uppercased()` uppercases the first character and `dropFirst()` keeps the rest of each split word.

## Run

    swift main.swift
