# 0062 — Do-While Loop

Use a do-while loop (whose body runs once before the condition is checked) to print `1`, `2`, and `3` on one line: `1 2 3`. Swift spells the do-while loop `repeat { ... } while cond`; the body always runs at least once (`do` is reserved for error handling).

## Run

    swift main.swift
