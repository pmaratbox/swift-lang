# 0560 — SHA-512

Compute the SHA-512 digest of the fixed string `hello` using Apple's CryptoKit library (`SHA512.hash(data:)`) and print it as a lowercase hex string with no separators. SHA-512 is deterministic, so the output is always the same for the same input.

## Run

    swift main.swift
