# 0129 — Add Days to a Date

Add 40 days to 2000-01-01 and print the result as `2000-02-10`. Swift's tuple return lets `civilFromDays` hand back (y, m, d) without a wrapper type.

## Run

    swift main.swift
