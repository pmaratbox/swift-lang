# 0370 — Quickselect

Find the 3rd smallest element of [7,10,4,3,20,15] with quickselect, printing `7`. Lomuto partition with `swapAt` recurses into only the side holding the k-th index.

## Run

    swift main.swift
