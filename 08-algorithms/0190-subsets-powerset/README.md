# 0190 — Subsets (Power Set)

Generate the power set of [1,2,3] in bitmask order 0..7 (empty printed as `{}`), one subset per line space-separated. For each mask 0..7, `filter` selects indices whose bit is set.

## Run

    swift main.swift
