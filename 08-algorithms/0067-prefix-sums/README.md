# 0067 — Prefix Sums

Compute the running totals (prefix sums) of `1, 2, 3, 4` — each element added to the sum of all the previous ones — and print them: `1 3 6 10`. `map` carrying a running `total` emits each prefix sum; Swift's standard library has no scan operator.

## Run

    swift main.swift
