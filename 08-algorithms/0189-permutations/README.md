# 0189 — Permutations

Generate all permutations of [1,2,3] in lexicographic order, one per line space-separated. Recursing over the sorted remaining elements yields lexicographic order naturally.

## Run

    swift main.swift
