# 0053 — Binary Search

Binary-search the sorted array `1, 3, 5, 7, 9` for `7` and print the index where it is found: `found 7 at index 3`. A hand-written loop over the array; Swift's stdlib has no binary search, though `firstIndex(of:)` is the linear fallback.

## Run

    swift main.swift
