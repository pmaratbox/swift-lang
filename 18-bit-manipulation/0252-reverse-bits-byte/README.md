# 0252 — Reverse Bits in a Byte

Reverse the 8 bits of the value 1 (00000001) to get 128 (10000000), printing `128`. Swift's `UInt8` keeps the value in a single byte during the shift loop.

## Run

    swift main.swift
