# 0259 — Enumerate Submasks

Enumerate all submasks of the mask 5 (101) in descending order `5 4 1 0`. Swift's `while true` with an explicit break captures the classic submask-enumeration loop.

## Run

    swift main.swift
