# 0262 — Parity Check

Compute the bit parity (XOR of all bits) of 7 (->1) and 5 (->0), printing `1 0`. Swift's `nonzeroBitCount & 1` yields the parity in one expression.

## Run

    swift main.swift
