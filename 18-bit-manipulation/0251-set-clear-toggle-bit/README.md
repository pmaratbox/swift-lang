# 0251 — Set, Clear, Toggle Bit

On bit position 1: set it on 0 (->2), clear it on 2 (->0), toggle it on 0 (->2), printing `2 0 2`. Swift uses the same `|`, `&`, `~`, `^` bitwise operators on `Int`.

## Run

    swift main.swift
