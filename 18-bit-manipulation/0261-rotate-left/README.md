# 0261 — Rotate Left (8-bit)

Rotate the 8-bit value 1 left by 1 (->2) and 128 left by 1 (->1), printing `2 1`. Swift's `UInt8` arithmetic confines the rotation to a single byte automatically.

## Run

    swift main.swift
