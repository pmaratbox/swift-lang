# 0313 — Level-Order Traversal

Print the breadth-first (level-order) traversal of the BST built from 5,3,8,1,4 `5 3 8 1 4`. Swift uses an array as a FIFO queue with a moving index to avoid costly front removals.

## Run

    swift main.swift
