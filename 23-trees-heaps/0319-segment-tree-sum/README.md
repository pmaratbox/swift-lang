# 0319 — Segment Tree Range Sum

Build a segment tree over [1,2,3,4,5] and query the sum of indices 1..3 (inclusive), printing `9`. Swift stores the iterative array-based segment tree in a `struct` and uses bitwise climbing for the range query.

## Run

    swift main.swift
