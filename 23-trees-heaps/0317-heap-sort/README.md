# 0317 — Heap Sort

Heap-sort [5,3,8,1,4] ascending, printing `1 3 4 5 8`. Swift uses `inout` array passing and `stride(from:through:by:)` to drive the build-heap and extraction phases.

## Run

    swift main.swift
