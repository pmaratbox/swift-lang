# 0318 — Kth Largest

Find the 2nd largest element of [3,2,1,5,6,4] using a heap, printing `5`. Swift keeps a bounded size-k min-heap `struct` whose root is the kth largest.

## Run

    swift main.swift
