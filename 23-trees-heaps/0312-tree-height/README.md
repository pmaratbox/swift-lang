# 0312 — Tree Height

Compute the height (in nodes) of the BST built from 5,3,8,1,4, printing `3`. Swift's `max` over recursive subtree heights makes the formula a one-liner.

## Run

    swift main.swift
