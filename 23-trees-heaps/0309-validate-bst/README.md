# 0309 — Validate BST

Check whether two trees are valid BSTs: a correct one (yes) and one with an out-of-place node (no), printing `yes no`. Swift recursion carries an (low,high) bound with optional `Node` references.

## Run

    swift main.swift
