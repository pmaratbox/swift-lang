# 0316 — Max-Heap Pop All

Build a max-heap from [3,1,4,1,5], pop the top three values, printing `5 4 3`. Swift wraps the array heap in a `struct` with `mutating` push/pop using `swapAt` for sift operations.

## Run

    swift main.swift
