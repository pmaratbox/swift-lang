# 0085 — Rotate a List

Rotate the list `1, 2, 3, 4, 5` left by `2` positions (elements wrap to the end) and print it: `3 4 5 1 2`. Slicing the tail `nums[k...]` and head `nums[..<k]` and concatenating the arrays rotates left.

## Run

    swift main.swift
