# 0097 — Enums with Associated Values

Define a shape type carrying associated data — `Rect(2, 3)` and `Square(4)` — compute each area by matching on the variant, and print `6` and `16`. Swift `enum` cases carry associated values; `switch` with `let` bindings destructures each case's payload.

## Run

    swift main.swift
