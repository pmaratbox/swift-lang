# 0346 — Newtype Wrapper

Wrap raw integers in distinct UserId and ProductId types so they cannot be confused, printing `user-1 prod-2`. Swift uses single-field structs as distinct newtypes the compiler keeps incompatible.

## Run

    swift main.swift
