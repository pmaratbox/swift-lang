# 0345 — Struct Defaults

Create a Point with default fields (0,0), then one overriding x to 5, printing each on its own line `0 0` then `5 0`. Swift's memberwise initializer honors stored-property default values, so `Point()` and `Point(x: 5)` both work.

## Run

    swift main.swift
