# 0343 — Optional Chaining

Read a deeply optional value a.b.c when present (5) and when absent (default 0), printing `5 0`. Swift chains with `?.` and supplies a fallback via the nil-coalescing operator `??`.

## Run

    swift main.swift
