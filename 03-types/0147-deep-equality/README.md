# 0147 — Deep Equality

Compare two nested structures with equal contents for structural equality and print `equal: yes`. Conforming a `struct` to `Equatable` lets the compiler synthesize a recursive, field-by-field `==`.

## Run

    swift main.swift
