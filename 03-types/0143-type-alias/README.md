# 0143 — Type Alias

Define a semantic alias for an integer type (e.g. Meters), assign 5, and print `distance: 5`. Swift's `typealias` introduces a name for an existing type with no runtime cost.

## Run

    swift main.swift
