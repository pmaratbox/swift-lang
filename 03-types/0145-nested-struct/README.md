# 0145 — Nested Struct

Model a person whose address is a nested struct, and print the city `London`. Each value-type `struct` nests by composition, reached with chained dot access (`person.address.city`).

## Run

    swift main.swift
