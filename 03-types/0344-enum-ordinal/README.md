# 0344 — Enum Ordinal

For the enum Direction(N,E,S,W) print the ordinal of S (2) and the name at ordinal 3 (W), printing `2 W`. Swift gives enums an `Int` raw value for the ordinal and `CaseIterable` for `allCases`.

## Run

    swift main.swift
