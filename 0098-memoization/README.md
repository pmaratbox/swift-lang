# 0098 — Memoization

Compute `fibonacci(10)` recursively with memoization (caching each result so it is computed once) and print it: `55`. A dictionary caches each result; the optional lookup `cache[n]` returns early on a hit.

## Run

    swift main.swift
