# 0091 — Balanced Parentheses

Using a stack, check whether `(())` is balanced and whether `(()` is balanced, printing `yes` then `no`. An array is the stack: `append` on `(`, `removeLast` on `)`; an empty array at the end means balanced.

## Run

    swift main.swift
