# 0294 — Subset Sum

Decide whether some subset of [3,34,4,12,5,2] sums to 9, printing `yes`. A boolean rolling array with reverse `stride` enforces single use of each number.

## Run

    swift main.swift
