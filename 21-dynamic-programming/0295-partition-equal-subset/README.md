# 0295 — Equal Partition

Decide whether [1,5,11,5] can split into two equal-sum subsets, printing `yes`. `reduce(0, +)` finds the total, reducing the problem to a subset-sum to half.

## Run

    swift main.swift
