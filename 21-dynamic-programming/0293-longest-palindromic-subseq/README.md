# 0293 — Longest Palindromic Subsequence

Compute the longest palindromic subsequence length of "bbbab", printing `4`. Converting the string to a `[Character]` array gives O(1) indexed access for the interval DP.

## Run

    swift main.swift
