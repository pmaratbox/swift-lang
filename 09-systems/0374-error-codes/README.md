# 0374 — Error Codes

Map the numeric error code 2 to its message "not found" and print `not found`. Swift uses a dictionary literal and nil-coalescing to resolve the code.

## Run

    swift main.swift
