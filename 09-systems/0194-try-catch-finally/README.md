# 0194 — Try / Catch / Finally

Throw and catch an error, printing `caught`, and always run a finally block printing `cleanup`, on two lines. Swift has no `finally`; a `defer` block runs on scope exit to provide the same guarantee.

## Run

    swift main.swift
