# 0302 — Kruskal MST

Compute the MST total weight of edges (0,1,1),(1,2,2),(0,2,3) with union-find, printing `3`. Swift's `mutating` DSU struct with path compression makes the union step concise.

## Run

    swift main.swift
