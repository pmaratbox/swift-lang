# 0083 — Sliding Window

Slide a window of size `2` over `1, 2, 3, 4` and print each window on its own line: `1 2`, `2 3`, `3 4`. The closed range `0...(count - size)` covers each valid start; slicing `nums[i..<i+size]` gives the window.

## Run

    swift main.swift
