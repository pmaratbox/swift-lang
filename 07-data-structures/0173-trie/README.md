# 0173 — Trie

Insert "cat" and "car" into a trie, then search "car" (yes) and "can" (no), printing `yes no`. A `[Character: TrieNode]` dictionary per node maps each edge, with an `isEnd` flag marking complete words.

## Run

    swift main.swift
