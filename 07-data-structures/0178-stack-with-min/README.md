# 0178 — Stack With Min

Build a stack that tracks its minimum in O(1); after pushing 3,1,2 print `min: 1`. A parallel min-stack records the running minimum so `getMin` is a constant-time array lookup.

## Run

    swift main.swift
