# 0365 — Multiset Counts

Add 1,1,2 to a multiset; print the count of 1 (2), remove one 1, then print the count of 1 (1), as `2 1`. Swift uses a Dictionary with the `[key, default:]` subscript to increment and decrement counts.

## Run

    swift main.swift
