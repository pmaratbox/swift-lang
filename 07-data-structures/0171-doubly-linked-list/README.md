# 0171 — Doubly Linked List

Build a doubly linked list 1<->2<->3, traverse forward then backward, printing `1 2 3` then `3 2 1` on two lines. A `final class` gives reference semantics so each node can hold optional `prev`/`next` links.

## Run

    swift main.swift
