# 0177 — Min Heap (Manual)

Implement a binary min-heap by hand, push 3,1,2, pop all, printing `1 2 3`. An array-backed heap with sift-up on push and sift-down on pop yields elements in ascending order.

## Run

    swift main.swift
