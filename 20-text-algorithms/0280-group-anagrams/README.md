# 0280 — Group Anagrams

Group ["eat","tea","tan","ate","nat"] into anagram classes and print the number of groups `2`. Keying a dictionary by `String(word.sorted())` collapses anagrams.

## Run

    swift main.swift
