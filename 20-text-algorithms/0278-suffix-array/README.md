# 0278 — Suffix Array

Build the suffix array of "banana" (sorted suffix start indices), printing `5 3 1 0 4 2`. Swift's `sorted(by:)` ranks indices with an inline suffix comparison.

## Run

    swift main.swift
