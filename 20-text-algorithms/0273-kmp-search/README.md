# 0273 — KMP Search

Use Knuth-Morris-Pratt to find all start indices of "ab" in "ababab", printing `0 2 4`. Swift arrays of `Character` make the prefix-function scan clean and index-safe.

## Run

    swift main.swift
