# 0284 — Trie Autocomplete

Insert "car","card","dog" into a trie and autocomplete the prefix "car", printing `car card`. Reference-type `final class` nodes with a `[Character: TrieNode]` map model the trie idiomatically.

## Run

    swift main.swift
