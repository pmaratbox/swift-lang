# 0283 — Rabin-Karp Search

Use rolling-hash Rabin-Karp to find all start indices of "ab" in "xabxab", printing `1 4`. Swift `asciiValue` plus modular arithmetic keeps the rolling hash exact.

## Run

    swift main.swift
