# 0001 — Hello, world!

The minimal Swift program: a single call to the global `print` function. A file
named `main.swift` may contain top-level statements that run in order, so no
`@main` type or explicit entry point is required. By default `print` appends a
newline, which gives us exactly one line of output.

## Run

    swift main.swift
