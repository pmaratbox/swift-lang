# 0361 — Frequency Sort

Sort [1,1,2,3,3,3] by descending frequency (ties keep first-seen order), printing `3 3 3 1 1 2`. Swift counts into a dictionary and sorts keys by `(count desc, first-seen index)`.

## Run

    swift main.swift
