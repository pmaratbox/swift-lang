# 0360 — Interleave Lists

Interleave [1,3,5] and [2,4,6] element by element, printing `1 2 3 4 5 6`. Swift pairs them with `zip` then `flatMap`s each pair into a flat sequence.

## Run

    swift main.swift
