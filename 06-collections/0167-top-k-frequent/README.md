# 0167 — Top-K Frequent

Find the 2 most frequent elements of [a,b,a,c,b,a], printing `a b`. Tally counts in a dictionary, then sort by descending count and take the prefix.

## Run

    swift main.swift
