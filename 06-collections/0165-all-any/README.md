# 0165 — All and Any

For [2,4,6] report whether all are even (yes) and whether any are odd (no), printing `yes no`. Swift offers `allSatisfy` and `contains(where:)` for these checks.

## Run

    swift main.swift
