# 0170 — Index By Key

Index people [(1,alice),(2,bob)] by id into a map, look up id 2, and print `id 2: bob`. `Dictionary(uniqueKeysWithValues:)` builds the index from key-value pairs.

## Run

    swift main.swift
