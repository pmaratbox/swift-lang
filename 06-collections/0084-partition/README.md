# 0084 — Partition

Partition `1, 2, 3, 4, 5, 6` into evens and odds, printing `evens: 2 4 6` and `odds: 1 3 5`. Two `filter` passes split the array; Swift's stdlib has no two-way partition returning both groups.

## Run

    swift main.swift
