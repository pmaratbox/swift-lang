# 0271 — DFA String Match

Build a DFA for the pattern "ab" and report the first match index in "aab", printing `1`. The DFA state counts matched pattern characters as it advances over the text.

## Run

    swift main.swift
