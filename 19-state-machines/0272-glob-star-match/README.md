# 0272 — Glob Star Match

Match the glob "a*b" (* = any run) against "aaab" (yes) and "aac" (no), printing `yes no`. Backtracking over `Character` arrays lets `*` consume zero or more characters.

## Run

    swift main.swift
