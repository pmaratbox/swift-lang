# 0142 — Countdown by Recursion

Recursively count down from 5 to 1, printing `5 4 3 2 1` on one line. Swift's `joined(separator:)` turns the collected values into one spaced line.

## Run

    swift main.swift
