# 0092 — Two Sum

Find the two indices in `2, 7, 11, 15` whose values sum to `9` and print them: `0 1`. A dictionary maps value to index; the optional lookup `seen[target - n]` returns a previously seen complement in one pass.

## Run

    swift main.swift
