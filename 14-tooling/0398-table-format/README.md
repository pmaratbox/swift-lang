# 0398 — Aligned Table

Format the rows (a,1),(bb,22) with the first column left-padded to the widest value, printing two aligned rows. Appending `String(repeating:count:)` spaces pads each first cell to the column width.

## Run

    swift main.swift
