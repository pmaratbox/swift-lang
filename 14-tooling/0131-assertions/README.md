# 0131 — Assertions

Assert several expected equalities (all true) and print `all passed`. A tiny `check` helper calls `fatalError` only on failure, so reaching the end means every fact held.

## Run

    swift main.swift
