# 0132 — Mini Test Runner

Run three named test functions that all pass and report `3 passed, 0 failed`. Each test is a named closure returning `Bool`, and the runner tallies the results into a summary.

## Run

    swift main.swift
