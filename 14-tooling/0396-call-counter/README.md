# 0396 — Call Counter

Wrap a function so each call increments a counter; after five calls print `calls: 5`. A small class captures the wrapped closure and tallies invocations.

## Run

    swift main.swift
