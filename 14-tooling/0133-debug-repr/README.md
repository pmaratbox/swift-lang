# 0133 — Debug Representation

Produce a debug/structured representation of a Point(1,2) and print `Point(x=1, y=2)`. Conforming to `CustomDebugStringConvertible` lets `String(reflecting:)` yield the exact structured form.

## Run

    swift main.swift
