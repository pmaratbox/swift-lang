# 0230 — Parse JSON Array

Parse the JSON array text "[1,2,3]" and print the sum of its elements `6`. A `compactMap`/`reduce` pipeline parses and sums in one expression.

## Run

    swift main.swift
