# 0232 — Word/Line/Char Counts

For the text "a b\nc" report words, lines, and characters, printing `3 2 5`. `split(whereSeparator:)` tokenizes on either whitespace character.

## Run

    swift main.swift
