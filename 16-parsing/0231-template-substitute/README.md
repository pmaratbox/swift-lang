# 0231 — Template Substitution

Substitute the variable in the template "hi {name}" with name="Ada", printing `hi Ada`. `replacingOccurrences(of:with:)` swaps each `{key}` placeholder.

## Run

    swift main.swift
