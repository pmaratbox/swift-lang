# 0225 — Recursive Descent Calculator

Parse and evaluate "2+3*4" honoring precedence, printing `14`. A `mutating` struct holds the cursor as the recursive grammar functions descend.

## Run

    swift main.swift
