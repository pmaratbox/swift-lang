# 0321 — Lazy Take

Build a lazy sequence of the natural numbers and take the first five, printing `1 2 3 4 5`. Swift's `sequence(first:next:)` yields a lazy stream you cap with `prefix`.

## Run

    swift main.swift
