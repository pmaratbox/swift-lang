# 0322 — Lazy Filter + Take

From a lazy stream of naturals, filter the even ones and take three, printing `2 4 6`. The `.lazy` view defers the filter so only what `prefix` needs is computed.

## Run

    swift main.swift
