# 0329 — CPS Factorial

Compute 5! in continuation-passing style, printing `120`. Each step takes a continuation closure, and the identity continuation extracts the result.

## Run

    swift main.swift
