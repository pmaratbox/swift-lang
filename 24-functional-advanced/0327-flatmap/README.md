# 0327 — FlatMap

FlatMap [1,2,3] with x -> [x, x*10] and print the flattened result `1 10 2 20 3 30`. Swift's `flatMap` maps each element to an array and concatenates the results.

## Run

    swift main.swift
