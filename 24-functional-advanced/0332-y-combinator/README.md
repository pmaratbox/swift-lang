# 0332 — Y Combinator

Define factorial via a fixed-point combinator (no named self-recursion) and compute 5!, printing `120`. A small `Rec` wrapper sidesteps Swift's ban on recursive closure types to build `fix`.

## Run

    swift main.swift
