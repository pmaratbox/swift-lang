# 0153 — Comparable Sort

Sort people [(alice,30),(bob,25)] by age ascending and print their names `bob alice`. Conforming to `Comparable` with a `<` operator on `age` lets `sorted()` order the values directly.

## Run

    swift main.swift
