# 0349 — Trait Composition

Compose two capabilities (A printing "a", B printing "b") into one type and invoke both, printing `a b`. In Swift, a type conforms to two protocols whose extensions supply each capability.

## Run

    swift main.swift
