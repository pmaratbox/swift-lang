# 0154 — Polymorphic Area

Sum the areas of a Rectangle(2,3)=6 and a Triangle(base=4,height=4)=8 through a common Shape interface, printing `total area: 14`. An array typed as `[Shape]` dispatches `area()` to each concrete struct at runtime through the protocol.

## Run

    swift main.swift
