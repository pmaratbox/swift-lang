# 0150 — Builder Pattern

Use a fluent builder to construct a Pizza of size M with cheese, and print `Pizza(M, cheese)`. Each builder method returns `self` so the calls chain fluently, and `CustomStringConvertible` supplies the final string form.

## Run

    swift main.swift
