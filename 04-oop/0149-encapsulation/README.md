# 0149 — Encapsulation

Keep an account balance private, deposit 50 onto an initial 100 through a method, and print `150`. Swift hides the stored property with the `private` access modifier and mutates it through a `mutating` method on the struct.

## Run

    swift main.swift
