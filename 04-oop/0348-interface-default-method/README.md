# 0348 — Interface Default Method

Define an interface with a default greet() returning "hi" and an implementer that overrides it to "hey", printing `hi hey`. In Swift, a protocol extension supplies the default method implementation.

## Run

    swift main.swift
