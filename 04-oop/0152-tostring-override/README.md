# 0152 — toString Override

Override the string conversion of a Point(1,2) so it prints `Point(1, 2)`. Conforming to `CustomStringConvertible` and supplying a `description` property is Swift's way to control how a value renders.

## Run

    swift main.swift
