# 0347 — Virtual Dispatch

Store a Circle, Square, and Triangle in one Shape collection and call describe() on each, printing `circle square triangle`. In Swift, class method overrides use dynamic dispatch through the base type.

## Run

    swift main.swift
