# 0079 — Character Codes

Print the character code of `A` and the character for code `66`: `65 B`. `Character.asciiValue` returns the ASCII code as a `UInt8`; `UnicodeScalar(66)` builds the scalar that becomes a `Character`.

## Run

    swift main.swift
